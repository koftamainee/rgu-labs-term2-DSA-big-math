#include "bigint.h"

bool operator==(bigint const &first, bigint const &second) {}
bool operator!=(bigint const &first, bigint const &second) {}

bool operator<(bigint const &first, bigint const &second) {}
bool operator<=(bigint const &first, bigint const &second) {}

bool operator>(bigint const &first, bigint const &second) {}
bool operator>=(bigint const &first, bigint const &second) {}

bigint bigint::operator~() {}
